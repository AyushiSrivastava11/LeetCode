class Solution {
    public boolean divisorGame(int n) {
       /*
        * Observe : if n = 1 means no alice does not win
        if n = 2 means alice wins
        */
        return (n%2==0);
    }
}